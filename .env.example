MONGO_USER=user
MONGO_PASSWORD=password
MONGO_HOST=localhost
MONGO_PORT=27017
MONGO_DB=db-name
# git variables to be used like this: git clone ${GIT_REPO}/${GIT_REPO_NAME}
GIT_REPO=git@github.com:example/user
GIT_REPO_NAME=your-repo-name # which is used in the git clone command and as the folder name
GRANDNODE_PROJECT_PATH=GrandNode.sln
GRANDNODE_WEB_PATH=path/to/Grand.Web